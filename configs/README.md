# Configs

This directory stores public example configuration files.

`train_multimodel_v2_balance.example.json` records the final balanced fine-tuning experiment parameters with placeholder relative paths. The current training entry uses command-line arguments rather than reading this JSON directly.

Do not store server addresses, account names, tokens, private paths, or private dataset locations here.
